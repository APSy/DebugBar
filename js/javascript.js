/**
 * Created by APSy on 06/06/14.
 */
var windowX = null,
    windowY = null;

var DebugBar = function(config) {

    this.defaultConfig = {
        'color':'#227799',
        'font-size':'12px', // max 20px
        'autoHide':false
    };

    this.view = function() {

        this.add = function() {
              var debugBarHTML = "";
        };
    };

    this.getAllIds = function() {

    },

    this.bindEvent = function(id) {

    };

    this.bindEvents = function(obj) {
        if(typeof obj == "object") {

        }
        return false;
    };

    this.unbindEvent = function(idOrClass) {

    }
};


var el = document.getElementById('toggle_get');
setTimeout(function() {
}, 1000);